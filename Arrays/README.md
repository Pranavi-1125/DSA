<h1>Notes<h1>

  
<p>At the end of solving each problem, if I learn something new like functions and concepts I will take a note here<p>

<h3>Two Sum<h3>
New : Sorting an array with a function

import java.util.Array;
arr = {4,32,6,7};
Arrays.sort(arr);
